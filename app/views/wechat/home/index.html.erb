<%= title '御易键' %>
<% if flash[:notice] %>
  <div class="notice"><%=flash[:notice]%></div>
<% end %>
<!-- <div class="home">
  <%= render 'shared/slide' %> -->

<!-- <% @type.each do |type| %>
  <div class='weui-panel__hd'><%= type.name %></div>
  <div class="weui-panel__bd">
    <div class="weui-grids">
      <% type.member_clubs.each do |ybclub| %>
        <a href="<%= wechat_member_card_path(ybclub)%>" class="weui-grid">
          <div class="weui-grid__icon">
            <%= image_tag(ybclub.icon_url, class: "weui-tabbar__icon") %>
          </div>
          <p class="weui-grid__label"><%= ybclub.name %></p>
        </a>
      <% end %>
    </div>
  </div>
<% end %> -->
  <div>
    <%= image_tag("home.jpg", class: "wechat_home_image") %>
  </div>

</div>
